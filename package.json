{
  "name": "kintai-note",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "wrangler dev",
    "deploy": "nuxt build && wrangler deploy",
    "db:apply": "wrangler d1 execute kintai-note --remote --file=db/schema.sql",
    "db:local:seed": "wrangler d1 execute kintai-note --local --file=db/seed.local.sql",
    "db:local:cleanup": "wrangler d1 execute kintai-note --local --file=db/cleanup.local.sql",
    "test:local": "node scripts/test-local.mjs"
  },
  "dependencies": {
    "@simplewebauthn/browser": "^13.1.1",
    "@simplewebauthn/server": "^14.0.2",
    "nuxt": "^3.17.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "wrangler": "^4.19.1"
  }
}
